root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/server"
  bin = "./tmp/main"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "ent/migrate", "vendor", "gen"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
